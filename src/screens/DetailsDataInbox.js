import React, { useState } from 'react';
import { View, Text } from 'react-native';

export default function DetailsDataInbox() {
    const [loading, setLoading] = useState(true);
    const [detailsData, setDetailsData] = useState(null);
    
    return (
        <View>
            <Text></Text>
        </View>
    )
}
